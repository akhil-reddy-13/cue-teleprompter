import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // The floating dev badge sits on top of the mobile tab bar.
  devIndicators: false,
};

export default nextConfig;
